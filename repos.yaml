repos:
  type: meta_repo
  git_org: geometry-labs
  repo_tree:

    # Frontend - private
    frontend: icon-tracker-frontend

    # Backend
    blocks: icon-blocks
    transactions: icon-transactions
    addresses: icon-addresses
    contracts: icon-contracts
    governance: icon-governance
    logs: icon-logs
#    tokens: icon-tokens

    exporter: icon-network-exporter

    etl: icon-etl

    load-tests: icon-explorer-load-tests

    # Prior backend - private
    .backend: icon-tracker-backend
